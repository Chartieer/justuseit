import * as React from "react";
import * as styles from "./{{ properCase name }}.scss";
import cx from 'classnames';

interface Props {
  children: React.ReactNode;
}

class {{ properCase name }} extends React.Component<Props, {}> {
  public render() {
    return (
      <div>
        {this.props.children}
      </div>
    );
  }
}

export default {{ properCase name }};
