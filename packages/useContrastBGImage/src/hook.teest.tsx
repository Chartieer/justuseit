import * as React from 'react';

import useCookie from './index';

describe(useCookie, () => {
  // test('should match snapshot and styles for default props', () => {
  // expect(mount(<Usetimer>hello</Usetimer>)).toMatchSnapshot();
  // });
});
